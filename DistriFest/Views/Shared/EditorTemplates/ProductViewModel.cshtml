@using Models
@model ProductViewModel
@Html.HiddenFor(x => x.ID, Model.ID)
@Html.HiddenFor(x => x.Name, Model.Name)
@Html.HiddenFor(x => x.Volume, Model.Volume)
@Html.HiddenFor(x => x.VolumeType, Model.VolumeType)
@Html.HiddenFor(x => x.Active, Model.Active)
@Html.HiddenFor(x => x.AmountInStock, Model.AmountInStock)
